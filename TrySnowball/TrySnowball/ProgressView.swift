import SwiftUI

struct ProgressView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Where are you at?")
                // Baby step selector here
            }
            .padding()
            .navigationTitle("Progress")
        }
    }
}
