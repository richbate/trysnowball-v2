import React from 'react';

export default function PaymentStrip({ total = 0, minimums = 0, boost = 0 }) {
 return (
  <div className="rounded-xl bg-slate-800/40 p-4 flex gap-6">
   <div>
    <div className="text-slate-400 text-xs">Total Balance</div>
    <div className="text-white text-lg">£{Math.round(total / 100)}</div>
   </div>
   <div>
    <div className="text-slate-400 text-xs">Monthly Minimums</div>
    <div className="text-white text-lg">£{Math.round(minimums / 100)}</div>
   </div>
   <div>
    <div className="text-slate-400 text-xs">Boost</div>
    <div className="text-white text-lg">£{Math.round(boost / 100)}</div>
   </div>
  </div>
 );
}
