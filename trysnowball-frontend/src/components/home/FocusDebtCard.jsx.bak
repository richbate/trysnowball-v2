import React from 'react';

export default function FocusDebtCard({ debtId, name, payoffMonthLabel, onAttackClick }) {
 return (
  <div className="rounded-xl bg-slate-800/40 p-4 flex items-center justify-between">
   <div>
    <div className="text-slate-400 text-xs">Focus Debt</div>
    <div className="text-white text-lg">{name || debtId || '—'}</div>
    <div className="text-slate-400 text-sm">Payoff: {payoffMonthLabel || '—'}</div>
   </div>
   <button
    onClick={() => onAttackClick?.(debtId)}
    className="px-3 py-2 rounded bg-blue-600 text-white text-sm"
   >
    Attack this debt
   </button>
  </div>
 );
}
