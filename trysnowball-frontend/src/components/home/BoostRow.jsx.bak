import React from 'react';

export default function BoostRow({ value = 0, presets = [], onChange, helperText }) {
 return (
  <div className="rounded-xl bg-slate-800/40 p-4 space-y-3">
   <div className="text-slate-200 text-sm">Add a monthly boost</div>
   <input
    type="range" min="0" max="50000" step="100"
    value={value}
    onChange={(e) => onChange?.(Number(e.target.value))}
    className="w-full"
   />
   <div className="flex gap-2 flex-wrap">
    {presets.map((p) => (
     <button
      key={p}
      onClick={() => onChange?.(p)}
      className="px-2 py-1 rounded bg-slate-700 text-slate-100 text-sm"
     >
      £{Math.round(p / 100)}
     </button>
    ))}
   </div>
   {helperText && <div className="text-slate-400 text-sm">{helperText}</div>}
  </div>
 );
}
