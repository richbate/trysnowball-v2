import React from 'react';

export default function ShareCard({
 variant = 'square', // 'og' | 'square' | 'story'
 headline = 'I just cleared a debt!',
 stat = '£500 wiped',
 subline = 'On my way to debt-free 🎉',
 footer = 'trysnowball.co.uk',
 theme = 'blue', // brand theme
 watermark = true
}) {
 const size = {
  og:   { w: 1200, h: 630 },
  square: { w: 1080, h: 1080 },
  story: { w: 1080, h: 1350 },
 }[variant];

 const { w, h } = size;

 return (
  <div
   id="share-card-root"
   style={{ width: w, height: h }}
   className="relative flex flex-col justify-between p-16 rounded-3xl overflow-hidden bg-gradient-to-br from-sky-100 via-white to-sky-200"
  >
   {/* subtle brand pattern */}
   <div 
    className="absolute inset-0 opacity-20 pointer-events-none"
    style={{ 
     backgroundImage: 'radial-gradient(circle at 20% 20%, rgba(0,0,0,0.04) 0 2px, transparent 2px)', 
     backgroundSize: '20px 20px' 
    }} 
   />
   
   {/* headline */}
   <div className="relative">
    <div className="text-5xl font-extrabold tracking-tight leading-tight text-gray-900">
     {headline}
    </div>
    <div className="text-2xl mt-4 opacity-80 text-gray-700">
     {subline}
    </div>
   </div>

   {/* stat block */}
   <div className="relative">
    <div className="text-8xl font-black leading-none text-sky-600">
     {stat}
    </div>
    <div className="mt-3 text-xl opacity-80 text-gray-600">
     with TrySnowball
    </div>
   </div>

   {/* footer */}
   <div className="relative flex items-center justify-between">
    <div className="text-xl font-semibold text-gray-800">
     {footer}
    </div>
    {watermark && (
     <div className="text-sm opacity-70 text-gray-600">
      #DebtFree #Snowball
     </div>
    )}
   </div>

   {/* corner snowball motif */}
   <div className="absolute -right-16 -bottom-16 w-72 h-72 rounded-full bg-sky-300 opacity-40 blur-2xl" />
  </div>
 );
}