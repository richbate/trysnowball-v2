/**
 * Security & Privacy Page
 * Explains data handling practices and privacy approach
 */

import React from 'react';
import { Link } from 'react-router-dom';
import DemoModeBanner from '../components/DemoModeBanner';
import { Shield, Monitor, Cloud, CheckCircle } from 'lucide-react';

const Security = () => {

 return (
  <div className={`min-h-screen ${colors.background}`}>
   <DemoModeBanner />
   
   <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {/* Header */}
    <div className="text-center mb-12">
     <div className="flex justify-center mb-6">
      <div className="bg-green-100 p-3 rounded-full">
       <Shield className="w-8 h-8 text-green-600" />
      </div>
     </div>
     <h1 className={`text-4xl font-bold ${colors.text.primary} mb-4`}>
      Your Data, Your Rules.
     </h1>
     <p className={`text-lg ${colors.text.secondary} max-w-2xl mx-auto`}>
      We take your privacy seriously — no marketing fluff, no hidden tracking.
     </p>
    </div>

    {/* Data Handling Options */}
    <div className="space-y-8 mb-12">
     {/* Demo Mode */}
     <div className={`${colors.surface} rounded-lg shadow-sm border ${colors.border} p-6`}>
      <div className="flex items-start gap-4">
       <div className="flex-shrink-0">
        <Monitor className="w-8 h-8 text-blue-500" />
       </div>
       <div className="flex-1">
        <h2 className={`text-xl font-semibold ${colors.text.primary} mb-3`}>
         🔒 Demo Mode
        </h2>
        <ul className="space-y-2 mb-4">
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          Your data stays in your browser
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          Nothing is uploaded or tracked
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          Complete privacy by design
         </li>
        </ul>
        <p className={`text-sm ${colors.text.secondary}`}>
         Perfect for exploring TrySnowball without any data concerns.
        </p>
       </div>
      </div>
     </div>

     {/* Logged-in Accounts */}
     <div className={`${colors.surface} rounded-lg shadow-sm border ${colors.border} p-6`}>
      <div className="flex items-start gap-4">
       <div className="flex-shrink-0">
        <Cloud className="w-8 h-8 text-purple-500" />
       </div>
       <div className="flex-1">
        <h2 className={`text-xl font-semibold ${colors.text.primary} mb-3`}>
         ☁️ Logged-in Accounts
        </h2>
        <ul className="space-y-2 mb-4">
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          <strong>AES-256-GCM encryption</strong> — every debt encrypted before storage
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          <strong>No plaintext creditor details</strong> — issuer names are hashed, balances encrypted
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          <strong>EU-hosted analytics</strong> — only de-identified amount bands (e.g. "2-5k")
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          Multi-device sync for your convenience
         </li>
         <li className={`flex items-start gap-3 text-sm ${colors.text.secondary}`}>
          <CheckCircle className="w-4 h-4 mt-1 text-green-500 flex-shrink-0" />
          We never sell or share your data — ever
         </li>
        </ul>
        <p className={`text-sm ${colors.text.secondary}`}>
         Bank-level encryption ensures your financial data stays private while enabling secure multi-device sync.
        </p>
       </div>
      </div>
     </div>
    </div>

    {/* Additional Protections */}
    <div className={`${colors.surface} rounded-lg p-8 ${colors.border} border mb-12`}>
     <h3 className={`text-xl font-semibold ${colors.text.primary} mb-6 text-center`}>
      Additional Protections
     </h3>
     
     <div className="grid md:grid-cols-2 gap-6">
      <div className="text-center">
       <div className="bg-blue-100 w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4">
        <Shield className="w-6 h-6 text-blue-600" />
       </div>
       <h4 className={`font-semibold ${colors.text.primary} mb-2`}>
        ✅ No Credit Checks
       </h4>
       <p className={`text-sm ${colors.text.secondary}`}>
        We don't link to your bank. You stay in control at all times.
       </p>
      </div>
      
      <div className="text-center">
       <div className="bg-green-100 w-12 h-12 rounded-full flex items-center justify-center mx-auto mb-4">
        <CheckCircle className="w-6 h-6 text-green-600" />
       </div>
       <h4 className={`font-semibold ${colors.text.primary} mb-2`}>
        No Third-Party Sharing
       </h4>
       <p className={`text-sm ${colors.text.secondary}`}>
        Your financial information stays between you and TrySnowball.
       </p>
      </div>
     </div>
    </div>

    {/* Privacy Policy CTA */}
    <div className="text-center">
     <p className={`${colors.text.secondary} mb-6`}>
      Want full details? Read our privacy policy
     </p>
     <div className="space-x-4">
      <Link
       to="/my-plan"
       className="inline-flex items-center px-6 py-3 bg-gray-100 text-gray-900 rounded-lg font-semibold hover:bg-gray-200 transition-colors"
      >
       Try Demo Mode
      </Link>
      <Link
       to="/onboarding"
       className="inline-flex items-center px-6 py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-colors"
      >
       Start Free
      </Link>
     </div>
    </div>
   </div>
  </div>
 );
};

export default Security;